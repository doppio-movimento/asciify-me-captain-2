const SideNavigator = (props) => {
    return (
        <div className="flex justify-center place-items-center bg-emerald-600 w-1/3 rounded">
            NAVIGATION
        </div>
    );
};

export default SideNavigator;
